This repo provides a boiler plate code for onion architecture with  dotnetcore using dapper. The database is postgresql. The application is expected to be deployed as container.
# DotnetCoreOnionArchDapper
